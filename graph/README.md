# read certain rrd fiiles in open-falcon graph modules and clear it. 

# prepare nodejs

```  $ npm install shelljs```

# prepare directory to store RRD files and RRD out files

```   $ mkdir RRD     ```  
```   $ mkdir RRDout  ```  
